CREATE TABLE IF NOT EXISTS `zotero` (
  `id` VARCHAR(16) NOT NULL,
  `authorTarget` BIT(1) NOT NULL,
  `targetId` INT NOT NULL,
  `authors` VARCHAR(300) NOT NULL,
  `title` VARCHAR(500) NOT NULL,
  `abstract` TEXT NULL,
  PRIMARY KEY (`id`));
