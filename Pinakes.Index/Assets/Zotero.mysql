CREATE TABLE IF NOT EXISTS `pix_zotero` (
  `id` int NOT NULL AUTO_INCREMENT,
  `zotero_id` VARCHAR(16) NOT NULL,
  `author_target` BIT(1) NOT NULL,
  `target_id` INT NOT NULL,
  `authors` VARCHAR(300) NOT NULL,
  `title` VARCHAR(500) NOT NULL,
  `abstract` TEXT NULL,
  PRIMARY KEY (`id`));
